# 爱莉唱歌插件 · 使用说明

## 一、这个插件能干什么

让你的 AI 角色真的唱歌给你听，不是念歌词。

你在 QQ 里说“唱一段小幸运”，一分钟内就能收到一条语音。你也可以指定唱副歌，或指定某几句。每次唱 20 秒左右的一小段。

## 二、开始之前，你需要准备火山引擎

插件只需要火山引擎一个平台。请在火山引擎控制台开通豆包实时语音、Seed TTS 和 SAUC 语音识别服务，创建或准备一个实时对话音色，并在“API Key 管理”中复制 API Key。

唱歌需要填写两个不同用途的音色，它们不能互相替代：

- `volc_speaker`：实时对话音色 ID，形如 `S_xxxxxxxx`，在火山控制台的声音复刻中训练得到。
- `volc_tts_speaker`：语音合成音色，填写豆包官方音色名，默认 `zh_female_vv_uranus_bigtts`，一般不用改。

把 `S_xxxxxxxx` 填到 `volc_tts_speaker` 会报 `resource ID is mismatched with speaker related resource`。三个服务共用同一个火山 API Key，相关服务按用量计费，请留意账单。

## 三、最关键的一步：训练角色的声音

声音是绑在你自己的火山引擎账号上的，别人的声音不能直接拿给你用，所以必须自己训练。

**最重要：训练必须使用唱歌的录音。**

如果你用说话的录音，角色只会念歌词，不会唱歌。请重点记住这一条。

准备素材时，建议找角色唱歌的音频，大约 8 分钟。声音越干净越好，最好没有伴奏。

如果手里的歌带伴奏，可以搜索“人声分离”工具，先把伴奏去掉。

开始训练时，看到“降噪”选项一定要关掉。开了以后，唱歌时的气声和颤音可能会被削掉，听起来不像人在唱歌。

训练完成后，你会得到一个声音编号，样子类似 `S_xxxxxxxx`。把它记下来，之后要填进插件设置。

## 四、安装步骤

1. 下载插件压缩包。
2. 解压后，把整个插件文件夹放进 AstrBot 的 `data/plugins/` 目录。
3. 按第五节“ffmpeg 安装引导”装好 ffmpeg。
4. 重启 AstrBot。
5. 打开 AstrBot 的网页管理界面，找到这个插件的设置。
6. 填入火山 API Key、实时对话音色 ID 和合成音色（默认值通常无需改动）。
7. 保存设置。

## 五、ffmpeg 安装引导

### 这是什么

ffmpeg 是一个免费的小工具，插件靠它处理和剪辑歌曲。你还需要同一套工具里的 `ffprobe`，它用来读取音频信息。

### Windows 怎么装

推荐先试简单方法：

1. 打开“命令提示符”。你可以按键盘上的 Windows 键，输入“命令提示符”，再打开它。
2. 输入下面这行并按回车：

   ```text
   winget install ffmpeg
   ```

3. 等它安装完成。安装过程中如果询问是否同意，按提示输入 `Y` 并回车。
4. 安装完成后，关闭命令提示符，再重新打开一个命令提示符，输入 `ffmpeg -version` 检查是否成功。

如果提示没有 `winget`，或者上面的方法没有装好，就用手动方法：

1. 打开 ffmpeg 官网的下载页：<https://ffmpeg.org/download.html>。
2. 在 Windows 区域选择 **gyan.dev** 提供的 Windows 构建版本。
3. 下载 `essentials` 版本的 ZIP 压缩包就够了，不需要下载 `full` 版本。
4. 把 ZIP 解压到一个固定位置，例如 `C:\ffmpeg`。解压后找到里面的 `bin` 文件夹。
5. **不用设置 Windows 环境变量。**打开插件设置，把下面两个完整路径分别填进去：
   - `ffmpeg_path` 填 `C:\ffmpeg\bin\ffmpeg.exe`
   - `ffprobe_path` 填 `C:\ffmpeg\bin\ffprobe.exe`
6. 保存插件设置，然后重启 AstrBot。

填路径时必须使用完整路径，并且要写到 `.exe` 结尾。你的解压文件夹名字如果和示例不同，就把示例中的 `C:\ffmpeg` 换成你实际解压的位置。

### Linux 怎么装

打开终端，根据你的系统输入对应的命令：

- Ubuntu / Debian：

  ```bash
  sudo apt install ffmpeg
  ```

- CentOS / RHEL / Fedora：

  ```bash
  sudo dnf install ffmpeg
  ```

  较旧的系统也可以试：

  ```bash
  sudo yum install ffmpeg
  ```

- Arch Linux：

  ```bash
  sudo pacman -S ffmpeg
  ```

安装完成后不用在插件设置里填路径，插件会自动找到它。

### 怎么确认装好了

打开命令提示符或终端，输入：

```text
ffmpeg -version
```

如果显示一大串版本信息，就是装好了。如果提示“找不到命令”或类似内容，就是还没装成，或者系统没有找到它。这时可以改用上面 Windows 手动方法中的完整路径，分别填写 `ffmpeg_path` 和 `ffprobe_path`。

### 注意

`ffmpeg` 和 `ffprobe` 是两个不同的文件，两个都需要。用包管理器安装时，通常会一起装好；手动解压时，请打开 `bin` 文件夹确认 `ffmpeg.exe` 和 `ffprobe.exe` 都在。

## 六、怎么用

在 QQ 里直接对机器人说：

- 你：“唱一段小幸运”
- 角色：“稍等一下。”
- 角色随后发来一条语音。

还可以这样说：

- “唱一下告白气球的副歌”——会专门剪副歌那段。
- “唱‘与你相遇好幸运’那句”——会剪出指定的那句。

角色会先回你一句，语音要等一会儿才到。快的话十几秒，慢的话一分多钟。

## 七、常见问题

**角色说不会唱这首歌**

火山引擎那边的歌曲库里没有这首歌。换一首试试。《后来》实测唱不了。

**唱出来是念歌词，不是唱歌**

训练声音时用了说话的录音。请重新准备唱歌的录音，再训练一次。

**听起来不太像人唱的，气声没了**

训练时“降噪”没有关掉。请重新训练，并确认“降噪”处于关闭状态。

**点了歌没有反应**

先等一分钟。还是没有反应，就检查火山 API Key 和两个音色 ID是否填错。

**提示要等冷却**

同一个人两次点歌之间要等 2 分钟，这是为了防止频繁点歌。

**偶尔唱了别的歌**

插件可能把歌名听错了，比如把“星晴”听成“心情”。换一种说法，再点一次。

**报错提到 ffmpeg**

说明 ffmpeg 没装好，或者 Windows 上没有填好它的完整路径。

## 八、几件要知道的事

- 每次只唱一小段，通常是 20 多秒，不是整首歌。
- 唱多长由唱歌服务决定，插件只能剪短，不能加长。
- 偶尔会失败，再点一次通常就好。
- 同时最多 2 个人排队。